import { useResource } from "../../../hooks/useResource";
import { getApiErrorMessage } from "../../../utils/apiError";

export const getTeacherApiError = getApiErrorMessage;
export const useTeacherResource = useResource;
