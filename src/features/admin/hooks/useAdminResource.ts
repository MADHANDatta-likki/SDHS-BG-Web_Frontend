import { useResource } from "../../../hooks/useResource";
import { getApiErrorMessage } from "../../../utils/apiError";

export const getAdminApiError = getApiErrorMessage;
export const useAdminResource = useResource;
